import { gsap } from "gsap";

export default function LPSection2() {
  return (
    <div className="h-screen">
      <section className=" h-screen flex justify-center items-start px-5 ">
        <h2 className="text-white text-4xl font-semibold text-center">
          You’ve probably wondered<br></br>that at some point.
        </h2>
      </section>
    </div>
  );
}
